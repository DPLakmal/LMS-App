const WeekPageLayout = ({ children }: { children: React.ReactNode }) => {
    return (

        <div className="">
            week page layout

            {children}
        </div>
    );
}

export default WeekPageLayout;
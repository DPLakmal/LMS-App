/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'th.bing.com',
      'upload.wikimedia.org',
      'foundr.com',
      'avatar.iran.liara.run',
    ],
  },
}

export default nextConfig
